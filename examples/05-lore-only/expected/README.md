# Expected output

Captured snapshots from a reference run of this example. Generated by running the example in its sandbox and saving output from:

- `guild lore catalog -p guild-example-05-lore-only` → `lore-catalog.txt`
- A sample `guild lore appraise` query → `appraise-sample.txt`

No quest-list snapshot — this example deliberately doesn't touch the quest axis.

<!-- TODO: populate snapshot .txt files once a reference run is captured -->
