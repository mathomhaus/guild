📋 Last briefing [2026-04-22T20:26] by agent:
  Rate-limiter planning done for guild-example-02-quest-decomposition. Integrated design = LORE-248: token-bucket on Redis, keyed by authenticated identity (not IP), explicit fail-open policy with local in-process fallback bucket, single emitted signal rate_limit_rejected_total{route,identity}. Sub-decisions feed in: LORE-244 (algorithm), LORE-245 (storage), LORE-246 (signal), LORE-247 (3 pitfalls). For whoever implements next: start with the Redis Lua token-bucket primitive — it's the atomic foundation the rest rides on. Then wire identity extraction (reject anonymous fallback to IP only as last resort), then add the local-fallback bucket path triggered on Redis errors, then emit the reject counter with both labels. Gotcha: LORE-241 and LORE-219 are prior near-duplicate "integrated design" entries from earlier runs — they're now stale; LORE-248 is the current source of truth, reforge only if it bothers you.
