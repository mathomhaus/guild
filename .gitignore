# Binaries
/guild
/sqlcheck
/docgen
/bin/
/dist/
*.exe
*.test
*.out

# Coverage
coverage.txt
coverage.html

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Go
vendor/

# Claude Code worktrees (transient agent-owned)
.claude/

# Model release cache (populated by `make assets-model` from a pinned
# model-v<semver> GitHub Release; see docs/MODEL.md).
/.cache/

# Recipe build workspace (populated by recipe/quantize.py; intermediate
# FP32 export plus the final int8 model.onnx).
/workspace/

# Python bytecode caches (recipe/ is the only py source tree).
__pycache__/
*.pyc
